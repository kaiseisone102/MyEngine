[編集前チェック] ソースを編集する前に確認 (Work_Protocol 正本):
- 推測禁止(§0-10): 触る関数/メンバ/struct/throw/include を実ソース Read で現物確認したか。読んだふり禁止。
- 怖い変更(§1-2): types.h struct / shader 全置換 / pipeline layout / pass_chain 配線 は変更前後を提示し承認を得たか。
- struct サイズ変更(§3-1a): types.h 等のサイズが変わるなら commit で必ず clean rebuild (--target clean → 再ビルド)。
- 反映確認(§3-1b): ビルド後 exe/.spv のタイムスタンプで焼き直しを確認 (error 0 だけで信用しない)。
- 起動確認(§3-2): validation/VUID/leaked を全行確認。描画変更は user 目視依頼。
- commit: 1 メッセージ 1 ツール。走らせて git rev-parse で hash 確認してから報告 (未実行を完了と書かない)。
- 設計判断は資料を引いてから(§0-8/§1-4): 答えが資料にあるなら二択を出さない。

[ツール発行と完了報告 (2026-05-30 事故で確立・最優先)]
- ツールは「本文に説明を書いてからツール」をやめ、説明文ゼロで 1 メッセージ 1 ツールだけ発行する。本文に
  court / <invoke ...> のようなツール呼び出し「文字」を書くと実際には発行されず空振りし、何も実行されないまま
  ターンがユーザーに戻る事故が多発した。複数ツールの並列も崩れる原因。1 個ずつ確実に発行する。
- この編集が「適用された」と扱う前に grep で内容を確認する。下記の通り Edit はブロックされ得るので、
  ブロックに気づかず未適用のまま「完了」と報告し中身の無い commit を作る事故を起こした (二度とやらない)。

[編集は時間差で成立する — 観測ベース・未検証注意]
- ※2026-05-30 に観測された挙動。フック仕様を実ソースで裏付けたものではない。弾かれたメッセージの指示を
  その場で読んで従うこと。
- 観測: この Edit/Write が「[BLOCKED ...] .claude/.docs_read_marker missing -> edit DENIED」で弾かれることがある。
- 観測した解除: 開発資料を読み該当ルールを引用 → PowerShell で marker 作成
  (New-Item -ItemType File -Force '<repo>\.claude\.docs_read_marker') → その後 Edit を再実行すると通った。
- ターンをまたぐと再びブロックされた (クリアの正確な契機は未検証)。再度ブロックされたら作り直す。
- 確実: ブロックされた Edit は未適用。grep で内容を確認してから「適用済み」と扱う。「完了」と書く前に
  必ず証拠 (grep / タイムスタンプ / validation 実行行 / git hash) を 1 つ出す。出せないなら「未確認」と書く。
