#pragma once
// =============================================================================
// core/obstacle.h — 静的障害物
// =============================================================================
// 木/墓/宝箱/岩 のような「通り抜けられないが地形ではない」 オブジェクト用。
// localAABB はモデルのローカル座標系の AABB (= Model::localAABB() の値)。
// 物理側で CTransform (pos + scale + yaw) を適用してワールド AABB にする。
//
// ObstacleTag: ECS クエリで「障害物全部」 を回す用。
// =============================================================================

#include "core/aabb.h"

struct CObstacle {
    AABB localAABB;
};

struct ObstacleTag {};
