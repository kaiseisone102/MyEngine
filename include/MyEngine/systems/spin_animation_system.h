#pragma once
// =============================================================================
// spin_animation_system.h — Y軸まわりの定常回転を適用するシステム
// =============================================================================
// CSpin + CTransform を持つ entity の yaw を毎フレーム加算する。
// 盾アイテムなどの装飾的回転や、 視認しやすさのための回転に使う。
// =============================================================================

struct WorldData;

class SpinAnimationSystem {
   public:
    void update(WorldData& data, float dt);
};
