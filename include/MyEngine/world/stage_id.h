#pragma once
// =============================================================================
// stage_id.h — ステージ識別子
// =============================================================================
// 新しいステージを追加するときは、 このenum に追加して
// stage_registry.cpp の kStageDefs に StageDef を追加するだけで対応可能。
// =============================================================================

enum class StageId : int {
    Terminal,    // ワープ集約地点
    Stage1_1,    // テスト用平地
    Stage1_2,    // テスト用斜面と穴
    Stage1_3,    // 崖と谷 (落下死テスト + ネズミ返し)
    Stage1_4,    // キノコ型 + 半月型 (START → END 約 300m)
    // Stage1_5, ... 追加自由
};
